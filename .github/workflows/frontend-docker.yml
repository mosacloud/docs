name: Build and push frontend Docker image

on:
  push:
    branches: [ main ]
    paths:
      - 'src/frontend/**'
      - '.github/workflows/frontend-docker.yml'

env:
  REGISTRY: docker.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Get package version
      id: package-version
      run: echo "version=$(jq -r .version src/frontend/apps/impress/package.json)" >> $GITHUB_OUTPUT

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/mosacloud/docs-frontend-mit
        tags: |
          type=raw,value=${{ steps.package-version.outputs.version }}-{{sha}}
          type=raw,value=latest,enable={{is_default_branch}}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./src/frontend/Dockerfile
        target: frontend-production
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        platforms: linux/amd64,linux/arm64
        build-args: |
          SW_DEACTIVATED=${{ vars.SW_DEACTIVATED || 'false' }}
          PUBLISH_AS_MIT=${{ vars.PUBLISH_AS_MIT || 'true' }}
          DOCKER_USER=1001
        cache-from: type=gha
        cache-to: type=gha,mode=max